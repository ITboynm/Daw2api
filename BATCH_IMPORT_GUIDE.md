# Factory Keys 批量导入指南

## 快速导入

### 方法 1：从 Excel/CSV 复制

1. 在 Excel 中准备你的 keys，一列一个：
```
fac_key_abc123
fac_key_def456
fac_key_ghi789
```

2. 全选并复制（Ctrl+C / Cmd+C）

3. 在管理后台：
   - 点击"添加 Key"
   - 勾选"批量添加模式"
   - 粘贴到"Keys"文本框
   - 设置名称前缀
   - 点击"批量添加"

### 方法 2：从文本文件导入

准备一个 `keys.txt` 文件：
```txt
fac_key_xxxxx1
fac_key_xxxxx2
fac_key_xxxxx3
fac_key_xxxxx4
fac_key_xxxxx5
```

打开文件，全选复制，然后粘贴到管理后台。

## 命名规范建议

### 按环境分类
```
生产环境:
前缀: Prod
序号: 1-50
结果: Prod-1, Prod-2, ..., Prod-50

测试环境:
前缀: Test
序号: 1-20
结果: Test-1, Test-2, ..., Test-20

开发环境:
前缀: Dev
序号: 1-10
结果: Dev-1, Dev-2, ..., Dev-10
```

### 按区域分类
```
美国区域:
前缀: US
序号: 1-30
结果: US-1, US-2, ..., US-30

欧洲区域:
前缀: EU
序号: 1-20
结果: EU-1, EU-2, ..., EU-20

亚洲区域:
前缀: ASIA
序号: 1-25
结果: ASIA-1, ASIA-2, ..., ASIA-25
```

### 按用途分类
```
API 服务:
前缀: API
序号: 1-40
结果: API-1, API-2, ..., API-40

数据处理:
前缀: Data
序号: 1-15
结果: Data-1, Data-2, ..., Data-15

客户服务:
前缀: Support
序号: 1-10
结果: Support-1, Support-2, ..., Support-10
```

## 批量导入示例

### 示例 1：导入 50 个生产环境 keys

**步骤：**
1. 准备 50 个 Factory API Keys
2. 复制所有 keys
3. 在管理后台：
   - 勾选"批量添加模式"
   - 粘贴 keys
   - 名称前缀：`Prod`
   - 起始序号：`1`
   - 点击"批量添加"

**结果：**
```
Prod-1  (fac_key_xxxxx1) - 活跃
Prod-2  (fac_key_xxxxx2) - 活跃
...
Prod-50 (fac_key_xxxxx50) - 活跃

✅ 成功添加 50 个 Factory Keys
```

### 示例 2：扩展现有 keys

**现有 keys：**
```
Prod-1 到 Prod-20 (已存在)
```

**新增 30 个：**
1. 复制新的 30 个 keys
2. 名称前缀：`Prod`
3. 起始序号：`21`（从现有的下一个序号开始）
4. 点击"批量添加"

**结果：**
```
Prod-1  到 Prod-20 (原有)
Prod-21 到 Prod-50 (新增)

✅ 总共 50 个 Factory Keys
```

### 示例 3：多环境混合导入

**第一批 - 生产环境（20个）：**
```
名称前缀: Prod
起始序号: 1
结果: Prod-1 到 Prod-20
```

**第二批 - 测试环境（10个）：**
```
名称前缀: Test
起始序号: 1
结果: Test-1 到 Test-10
```

**第三批 - 开发环境（5个）：**
```
名称前缀: Dev
起始序号: 1
结果: Dev-1 到 Dev-5
```

## 导入验证

### 导入后检查清单

✅ **数量核对**
- 检查添加的数量是否正确
- 在 Factory Keys 列表中查看总数

✅ **名称验证**
- 确认名称前缀正确
- 确认序号连续无遗漏

✅ **状态检查**
- 所有 keys 状态为"活跃"
- 失败计数为 0

✅ **功能测试**
- 进行一次 API 调用测试
- 查看仪表板确认当前使用的 key

## 常见问题

### Q1: 粘贴的 keys 中有空行怎么办？
**A:** 系统会自动过滤空行，不影响导入。

### Q2: 如果某个 key 格式不正确？
**A:** 该 key 会被跳过，其他正确的 keys 会正常添加。

### Q3: 批量添加后能修改名称吗？
**A:** 目前不支持批量修改名称，但可以单个编辑或删除后重新添加。

### Q4: 导入大量 keys 会很慢吗？
**A:** 一般情况下很快，建议单次导入不超过 100 个。

### Q5: 可以导入重复的 key 吗？
**A:** 可以导入相同的 key，但会创建多个记录，不建议这样做。

## 最佳实践

### 1. 提前规划
- 确定命名规范
- 计算需要的数量
- 预留扩展空间

### 2. 分批导入
```
第一批: 核心生产 keys (20个)
第二批: 备用生产 keys (10个)
第三批: 测试和开发 keys (15个)
```

### 3. 文档记录
创建一个记录表格：
```
环境 | 前缀 | 序号范围 | 数量 | 用途
-----|------|----------|------|------
生产 | Prod | 1-20     | 20   | 主要服务
生产 | Prod | 21-30    | 10   | 备用
测试 | Test | 1-10     | 10   | 测试环境
开发 | Dev  | 1-5      | 5    | 开发测试
```

### 4. 定期维护
- 检查 key 状态
- 清理失效的 keys
- 补充新的 keys
- 更新文档记录

## 高级技巧

### 技巧 1：使用脚本生成 keys 列表
```bash
# 示例：生成测试用的 key 列表
for i in {1..50}; do
  echo "fac_test_key_$(date +%s)_$i"
done > keys.txt
```

### 技巧 2：Excel 公式批量处理
```excel
=CONCATENATE("fac_key_", TEXT(ROW()-1, "0000"))
```

### 技巧 3：按性能分组
```
高性能 keys: HP-1 到 HP-20
标准 keys: STD-1 到 STD-30  
备用 keys: BKP-1 到 BKP-10
```

### 技巧 4：时间戳命名
```
前缀: Key-20251029
序号: 1-20
结果: Key-20251029-1, Key-20251029-2, ...
```

这样可以通过名称快速识别导入时间。

## 总结

批量导入功能让你可以：
- ⚡ 快速导入大量 keys
- 🏷️ 自动生成规范的名称
- 📊 统一管理和监控
- 🔄 轻松扩展和维护

**记住：** 合理的命名规范和文档记录是成功管理大量 keys 的关键！

---

**提示**: 如有任何问题，请查看错误日志或联系技术支持。

